services:
  grafana:
    image: grafana/grafana

    container_name: mercadinho-grafana

    ports:
      - '3001:3000'

    networks:
      - mercadinho-network

networks:
  mercadinho-network:
    driver: bridge